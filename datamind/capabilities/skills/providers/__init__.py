"""Skills providers — currently the loader has no pluggable parts,
but we reserve this package so third parties can register new skill sources
(e.g. remote registries, signed bundles) later under the same pattern."""
